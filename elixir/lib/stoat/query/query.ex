defmodule Stoat.Query do

  # not 100% sure how this can be written
  # Maybe aggregates need to be stored as ecto schemas with helper wrappers?
  # That way we can query them without the overhead of writing a query engine ourself


end
